function printNtoOne(arr) {

    /* Get N from the array */
    let n = Number(arr[0]);

    /* For loop to print every number from n to 1 */
    for (i = n; i >= 1; i--) {
        console.log(i);
    }
}
