function printReversed(arr) {

    /* Create new array with the same elements of arr but reversed */
    let reversed = arr.reverse();

    /* Loop through each element in the reversed array */
    for (let currentnum of reversed)
        console.log(currentnum);

}
