function calcExpr() {
    /* Expression to be solved */
    let expression = (30 +25) * 1/3 * (35 - 14 - 12);
    /* Power the solved expression by specific number */
    let output = Math.pow(expression, 2);
    console.log(output);
}